# Recall — Environment Variables
# Copy this file to .env and fill in values as needed.
# .env is gitignored and will never be committed.

# ── Option A: Cloud inference (no GPU needed) ──────────────────────────────
# Get a key at https://openrouter.ai/keys
# The agent will use this automatically if no local model server is running.
OPENROUTER_API_KEY=

# ── Option B: Local vLLM (Linux with GPU) ──────────────────────────────────
# Leave blank if using macOS MLX or OpenRouter
VLLM_HOST=0.0.0.0
VLLM_PORT=8000

# ── Logging ────────────────────────────────────────────────────────────────
# Set to DEBUG for verbose output when troubleshooting
FASTMCP_LOG_LEVEL=INFO

# ── Transport ──────────────────────────────────────────────────────────────
# Options: stdio (Claude Desktop), http (ChatGPT), sse
MCP_TRANSPORT=stdio
